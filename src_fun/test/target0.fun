#!/home/kalinovcic/schedulebot/run_tree/fun

run
{
    /*foo: ()
    {
    };*/

    c := cast(u64, POO * 3);
    b := cast(u64, POO * 2);
    a := cast(u64, POO :: 123);
    debug a;
    debug b;
    debug c;

    /*
    poo1: void;
    poo2: void;
    poo1 = poo2;
    debug FF :: 42069e1000 / 59012;

    bla: s8 = cast(s8, -128);
    debug bla;

    if cast(s64, 5)
    {
        debug cast(s64, 5);
    }
    else
    {
        debug cast(s64, 15);
    }
    */

    /*a: f32 = 123.45e+6;
    if a > 1e100 [[unlikely]]
    {
    }

    yield void;

    exit: u32 = 60;
    code: u32 = 69;
    syscall(exit, code);*/
}

